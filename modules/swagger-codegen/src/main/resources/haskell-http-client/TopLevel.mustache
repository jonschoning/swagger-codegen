{-|
Module : {{title}}
-}

module {{title}}
  ( module {{title}}.Client
  , module {{title}}.API
  , module {{title}}.Model
  , {{#generateLenses}}module {{title}}.Lens{{/generateLenses}}
  ) where

import {{title}}.API
import {{title}}.Client
import {{title}}.Model
{{#generateLenses}}import {{title}}.Lens{{/generateLenses}}
